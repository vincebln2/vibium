# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/
.gemini/
.claude/
.agents/

# Node.js
node_modules/

# TypeScript build output
dist/

# Go binaries
clicker/bin/
clicker/clicker

# Embedded skills (copied from skills/browser/ and skills/check/ at build time)
clicker/cmd/clicker/SKILL.md
clicker/cmd/clicker/CHECK_SKILL.md
clicker/cmd/clicker/AI_ENV_TEMPLATE
clicker/cmd/clicker/CLOUD_ENV_TEMPLATE

# Platform package binaries (generated by make package-js)
packages/*/bin/
!packages/vibium/bin/

# macOS
.DS_Store

# Test screenshots
screenshots/

# Recording/trace artifacts
*-record.zip
*-trace.zip
saucedemo-record/

# External reference checkouts (root only — an unanchored pattern also
# swallows docs/reference/, so new docs there could never be added)
/reference/

# Claude Code skill install state (local-only; no companion manifest)
skills-lock.json

# Local-only deps for scripts/ utilities (e.g. playwright for the
# trace comparison harness — install with: cd scripts && npm install playwright)
scripts/package.json
scripts/package-lock.json

# NPM package tarballs
*.tgz

# Java / Gradle
clients/java/build/
clients/java/.gradle/
clients/java/src/main/resources/natives/
clients/java/src/main/resources/vibium-version.txt
vibium-bundle.zip

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv*/
venv/
.eggs/
*.whl

# Python platform package binaries (generated by make package-python)
packages/python/*/src/*/bin/vibium
packages/python/*/src/*/bin/vibium.exe

# LICENSE/NOTICE copies in subdirectories (generated at publish time)
**/LICENSE
**/NOTICE
!/LICENSE
!/NOTICE
*.swp

# Per-process capability counts collected during a test run
tests/.capability-summary/
